<Page
    x:Class="XKCB.DetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:XKCB"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Black">

    <Grid Background="Black">

        <!-- 1. BACKGROUND LAYER (No Gradients, Right-aligned 90% width) -->
        <Grid>
            <Image Grid.Column="1" x:Name="BackdropImage" Stretch="UniformToFill" HorizontalAlignment="Stretch" VerticalAlignment="Top" Height="768" Opacity="0.2" />
        </Grid>

        <!-- 2. CONTENT LAYER (Vertically Centered Layout) -->
        <Grid Padding="60,0,10,0" MaxWidth="1920" HorizontalAlignment="Stretch" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Poster Area -->
            <Border Grid.Column="0" Width="420" Height="630" CornerRadius="8" Margin="0,60,100,0" VerticalAlignment="Center">
                <Border.Shadow>
                    <ThemeShadow />
                </Border.Shadow>
                <Border.Background>
                    <ImageBrush x:Name="PosterImageBrush" Stretch="UniformToFill" />
                </Border.Background>
            </Border>

            <!-- Text & Actions Area -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">

                <!-- Title -->
                <TextBlock x:Name="TitleText" FontSize="64" FontWeight="Bold" Foreground="White" TextWrapping="Wrap" Margin="0,300,0,0">
                    <TextBlock.Shadow>
                        <ThemeShadow />
                    </TextBlock.Shadow>
                </TextBlock>

                <!-- Meta Data (Year, Rating) -->
                <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,0,0,20">
                    <TextBlock x:Name="YearText" FontSize="15" Foreground="#D4D4D4"/>
                    <TextBlock Text="•" FontSize="15" Foreground="#525252"/>
                    <TextBlock x:Name="RatingText" FontSize="15" Foreground="#FACC15" FontWeight="SemiBold"/>
                </StackPanel>

                <!-- Sources Container -->
                <StackPanel x:Name="SourcesSection" Visibility="Collapsed" Background="#000000" Padding="24" CornerRadius="8" BorderBrush="#1A1A1A" BorderThickness="1" HorizontalAlignment="Left" MinWidth="750">
                    <TextBlock Text="SELECT SOURCE" FontSize="12" FontFamily="Consolas" Foreground="#737373" Margin="0,0,0,16" CharacterSpacing="100"/>

                    <ItemsControl x:Name="SourcesList">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapGrid Orientation="Horizontal" MaximumRowsOrColumns="-1" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="local:MovieSource">
                                <Button Click="PlayButton_Click" Tag="{x:Bind Id}" 
                                        Background="Transparent" BorderBrush="#2A2A2A" BorderThickness="1" CornerRadius="6" 
                                        Padding="24,12" Margin="0,0,16,16" MinWidth="160">
                                    <TextBlock Text="{x:Bind VariationName}" Foreground="#E5E5E5" FontFamily="Consolas" FontSize="14" CharacterSpacing="50" HorizontalAlignment="Center"/>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

                <!-- Seasons Container -->
                <StackPanel x:Name="SeasonsSection" Visibility="Collapsed" Background="#080808" Padding="24" CornerRadius="8" BorderBrush="#1A1A1A" BorderThickness="1" HorizontalAlignment="Left" MinWidth="750">
                    <TextBlock Text="SELECT SEASON" FontSize="12" FontFamily="Consolas" Foreground="#737373" Margin="0,0,0,16" CharacterSpacing="100"/>

                    <ItemsControl x:Name="SeasonsList">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapGrid Orientation="Horizontal" MaximumRowsOrColumns="-1" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="Season" Background="Transparent" BorderBrush="#2A2A2A" BorderThickness="1" CornerRadius="6" Padding="24,12" Margin="0,0,16,16" Foreground="#E5E5E5" FontFamily="Consolas" FontSize="14" CharacterSpacing="50"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

            </StackPanel>
        </Grid>

        <!-- Back Button -->
        <Button Content="← Back" Click="BackButton_Click" Style="{ThemeResource TextBlockButtonStyle}" Foreground="#A3A3A3" FontFamily="Consolas" FontSize="12" CharacterSpacing="100" Margin="40,40,0,0" VerticalAlignment="Top" HorizontalAlignment="Left"/>
    </Grid>
</Page>